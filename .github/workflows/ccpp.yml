name: C++ CI

on: 
  push:
    branches:
    - develop
    - master

jobs:

  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
